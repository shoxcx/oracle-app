const { app, BrowserWindow } = require('electron'); const fs = require('fs'); app.whenReady().then(async () => { const win = new BrowserWindow({show: false}); await win.loadURL('https://u.gg/lol/champions/ahri/build/mid'); setTimeout(async () => { const html = await win.webContents.executeJavaScript('document.body.innerHTML'); fs.writeFileSync('ugg.html', html); app.quit(); }, 8000); });
